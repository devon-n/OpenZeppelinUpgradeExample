# Simple example of upgrading contracts using open zeppelin

Upgrading a contract that holds a variable by adding a function to it


```
npm i | yarn add
npx hardhat test
npx hardhat run scripts/deploy.js
```